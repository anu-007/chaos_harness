[pytest]
# The invariant suite is entirely async (asyncpg against the compose Postgres), so run every
# coroutine test on the event loop automatically without per-test @pytest.mark.asyncio.
asyncio_mode = auto
testpaths = tests
